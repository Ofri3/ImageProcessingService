pipeline {
    agent any
    stages {
        stage('Deploy to VM') {
            steps {
                    bat """
                        echo deploy k8s cluster (or any other alternative...)
                    """
            }
        }
    }
}